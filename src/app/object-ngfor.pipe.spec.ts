import { ObjectNgforPipe } from './object-ngfor.pipe';

describe('ObjectNgforPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectNgforPipe();
    expect(pipe).toBeTruthy();
  });
});
